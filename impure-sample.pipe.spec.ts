import { ImpureSamplePipe } from './impure-sample.pipe';

describe('ImpureSamplePipe', () => {
  it('create an instance', () => {
    const pipe = new ImpureSamplePipe();
    expect(pipe).toBeTruthy();
  });
});
